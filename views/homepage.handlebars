<h2>this is the homepage.handlebars view. It was inserted to the main view with a res.render() after a get request for '/'</h2>

<p>
This repo is set up to use express framework for creating web apps. </br> 
It uses mySQL and Sequelize to create databases. </br> 
The server.js file is set up to be served on heroku or locally. </br> 
I created this as a template to start server projects more easily and to wrap my head around what this code is actually doing.

</p>

<p>
    Helper test should show '3': </br>
    <!--this calls a helper function-->
    {{sample_function 1  2}}
</p>

<h2>All Plants Object</h2>
{{#each plants as |plant|}}
<p>{{plant.common_name}}</p>
{{/each}}

<h2>All users</h2>
{{#each users as |user|}}
<p>{{user.username}}</p>
{{/each}}

<h2>All User Plants</h2>
{{#each userPlants as |userPlant|}}
<p>
    Name: {{userPlant.nickname}} </br>
     Plant ID: {{userPlant.plant_id}} </br>
      Plant Type: {{userPlant.plant.common_name}} </br>
     user ID: {{userPlant.user_id}}
</p>
{{/each}}



        