<main>
  <div class="plant-grid">
    <div class="card">
      <a class="add-plant" href="#">+ Add Plant</a>
    </div>

    {{#if userPlants.length}}

    {{#each userPlants as |userPlant|}}

    {{> plant-card userPlant}}

    {{/each}}

    {{else}}

    <div class="card">
    </div>
    <div class="card">
    </div>
    <div class="card">
    </div>
  </div>

  {{/if}}

  </div>
  <div class="schedule">
    <h1>Schedule</h1>
    <!-- This is where we'll add Schedule list item logic -->
    {{#if userPlants.length}}
    {{> list-items userPlants}}
    {{else}}
    <ul>
      <li>No Tasks</li>
    </ul>
    {{/if}}
  </div>
</main>